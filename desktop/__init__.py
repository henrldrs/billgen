"""Desktop-mode support: local SQLite sidecar bootstrap, licensing, paths.

Toolchain-independent (pure Python). The Tauri shell in frontend-electron/
spawns `python -m desktop.bootstrap` as its API sidecar."""
